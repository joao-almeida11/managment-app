// import app from "@api/server"; // or wherever you export your express app
// import request from "supertest";

// tes t("POST /tasks creates a task", async () => {
//   prisma.task.create.mockResolvedValue(mockTask); // same mock

//   const res = await request(app).post("/tasks").send(newTask);

//   expect(res.status).toBe(201);
// });
